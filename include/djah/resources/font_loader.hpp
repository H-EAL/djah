#ifndef DJAH_RESOURCES_FONT_LOADER_HPP
#define DJAH_RESOURCES_FONT_LOADER_HPP

/*
#include <ft2build.h>
#include FT_FREETYPE_H

#include "loader.hpp"
#include "font.hpp"
*/

namespace djah { namespace resources {

	/*
	class font_loader
		: public loader<font>
	{
	public:
		font_loader();
		~font_loader();
		font* loadFromStream(filesystem::stream &stream, const std::string &filename = "");

	private:
		static FT_Library s_library;
		static bool s_lib_initialized;
		static 
		const char *s_char_map;
	};
	*/
} /*resources*/ } /*djah*/

#endif /* DJAH_RESOURCE_FONT_LOADER_HPP */